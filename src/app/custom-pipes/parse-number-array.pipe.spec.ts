import { ParseNumberArrayPipe } from './parse-number-array.pipe';

describe('ParseNumberArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseNumberArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
